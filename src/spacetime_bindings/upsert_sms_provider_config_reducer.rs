// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpsertSmsProviderConfigArgs {
    pub api_host: String,
    pub app_id: String,
    pub merchant_id: String,
    pub version: String,
    pub sign_type: String,
    pub secret_key: String,
    pub template_id: String,
    pub message_type: String,
    pub code_pepper: String,
}

impl From<UpsertSmsProviderConfigArgs> for super::Reducer {
    fn from(args: UpsertSmsProviderConfigArgs) -> Self {
        Self::UpsertSmsProviderConfig {
            api_host: args.api_host,
            app_id: args.app_id,
            merchant_id: args.merchant_id,
            version: args.version,
            sign_type: args.sign_type,
            secret_key: args.secret_key,
            template_id: args.template_id,
            message_type: args.message_type,
            code_pepper: args.code_pepper,
        }
    }
}

impl __sdk::InModule for UpsertSmsProviderConfigArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `upsert_sms_provider_config`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait upsert_sms_provider_config {
    /// Request that the remote module invoke the reducer `upsert_sms_provider_config` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`upsert_sms_provider_config:upsert_sms_provider_config_then`] to run a callback after the reducer completes.
    fn upsert_sms_provider_config(
        &self,
        api_host: String,
        app_id: String,
        merchant_id: String,
        version: String,
        sign_type: String,
        secret_key: String,
        template_id: String,
        message_type: String,
        code_pepper: String,
    ) -> __sdk::Result<()> {
        self.upsert_sms_provider_config_then(
            api_host,
            app_id,
            merchant_id,
            version,
            sign_type,
            secret_key,
            template_id,
            message_type,
            code_pepper,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `upsert_sms_provider_config` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn upsert_sms_provider_config_then(
        &self,
        api_host: String,
        app_id: String,
        merchant_id: String,
        version: String,
        sign_type: String,
        secret_key: String,
        template_id: String,
        message_type: String,
        code_pepper: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl upsert_sms_provider_config for super::RemoteReducers {
    fn upsert_sms_provider_config_then(
        &self,
        api_host: String,
        app_id: String,
        merchant_id: String,
        version: String,
        sign_type: String,
        secret_key: String,
        template_id: String,
        message_type: String,
        code_pepper: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpsertSmsProviderConfigArgs {
                api_host,
                app_id,
                merchant_id,
                version,
                sign_type,
                secret_key,
                template_id,
                message_type,
                code_pepper,
            },
            callback,
        )
    }
}
