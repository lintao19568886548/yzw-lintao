// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::collection_sms_batch_result_type::CollectionSmsBatchResult;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
struct SendCollectionSmsArgs {
    pub bill_ids: Vec<u64>,
    pub collection_type: String,
    pub due_date: String,
    pub overdue_days: u32,
}

impl __sdk::InModule for SendCollectionSmsArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the procedure `send_collection_sms`.
///
/// Implemented for [`super::RemoteProcedures`].
pub trait send_collection_sms {
    fn send_collection_sms(
        &self,
        bill_ids: Vec<u64>,
        collection_type: String,
        due_date: String,
        overdue_days: u32,
    ) {
        self.send_collection_sms_then(bill_ids, collection_type, due_date, overdue_days, |_, _| {});
    }

    fn send_collection_sms_then(
        &self,
        bill_ids: Vec<u64>,
        collection_type: String,
        due_date: String,
        overdue_days: u32,

        __callback: impl FnOnce(
                &super::ProcedureEventContext,
                Result<CollectionSmsBatchResult, __sdk::InternalError>,
            ) + Send
            + 'static,
    );
}

impl send_collection_sms for super::RemoteProcedures {
    fn send_collection_sms_then(
        &self,
        bill_ids: Vec<u64>,
        collection_type: String,
        due_date: String,
        overdue_days: u32,

        __callback: impl FnOnce(
                &super::ProcedureEventContext,
                Result<CollectionSmsBatchResult, __sdk::InternalError>,
            ) + Send
            + 'static,
    ) {
        self.imp
            .invoke_procedure_with_callback::<_, CollectionSmsBatchResult>(
                "send_collection_sms",
                SendCollectionSmsArgs {
                    bill_ids,
                    collection_type,
                    due_date,
                    overdue_days,
                },
                __callback,
            );
    }
}
