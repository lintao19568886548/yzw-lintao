// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct AuditReimbursementArgs {
    pub id: u64,
    pub status: i8,
    pub audit_opinion: Option<String>,
}

impl From<AuditReimbursementArgs> for super::Reducer {
    fn from(args: AuditReimbursementArgs) -> Self {
        Self::AuditReimbursement {
            id: args.id,
            status: args.status,
            audit_opinion: args.audit_opinion,
        }
    }
}

impl __sdk::InModule for AuditReimbursementArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `audit_reimbursement`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait audit_reimbursement {
    /// Request that the remote module invoke the reducer `audit_reimbursement` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`audit_reimbursement:audit_reimbursement_then`] to run a callback after the reducer completes.
    fn audit_reimbursement(
        &self,
        id: u64,
        status: i8,
        audit_opinion: Option<String>,
    ) -> __sdk::Result<()> {
        self.audit_reimbursement_then(id, status, audit_opinion, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `audit_reimbursement` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn audit_reimbursement_then(
        &self,
        id: u64,
        status: i8,
        audit_opinion: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl audit_reimbursement for super::RemoteReducers {
    fn audit_reimbursement_then(
        &self,
        id: u64,
        status: i8,
        audit_opinion: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            AuditReimbursementArgs {
                id,
                status,
                audit_opinion,
            },
            callback,
        )
    }
}
