// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct CreateCrawlerTaskArgs {
    pub source_id: u64,
    pub task_type: String,
    pub max_retry_count: u32,
    pub request_config_json: Option<String>,
}

impl From<CreateCrawlerTaskArgs> for super::Reducer {
    fn from(args: CreateCrawlerTaskArgs) -> Self {
        Self::CreateCrawlerTask {
            source_id: args.source_id,
            task_type: args.task_type,
            max_retry_count: args.max_retry_count,
            request_config_json: args.request_config_json,
        }
    }
}

impl __sdk::InModule for CreateCrawlerTaskArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `create_crawler_task`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait create_crawler_task {
    /// Request that the remote module invoke the reducer `create_crawler_task` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`create_crawler_task:create_crawler_task_then`] to run a callback after the reducer completes.
    fn create_crawler_task(
        &self,
        source_id: u64,
        task_type: String,
        max_retry_count: u32,
        request_config_json: Option<String>,
    ) -> __sdk::Result<()> {
        self.create_crawler_task_then(
            source_id,
            task_type,
            max_retry_count,
            request_config_json,
            |_, _| {},
        )
    }

    /// Request that the remote module invoke the reducer `create_crawler_task` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn create_crawler_task_then(
        &self,
        source_id: u64,
        task_type: String,
        max_retry_count: u32,
        request_config_json: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl create_crawler_task for super::RemoteReducers {
    fn create_crawler_task_then(
        &self,
        source_id: u64,
        task_type: String,
        max_retry_count: u32,
        request_config_json: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            CreateCrawlerTaskArgs {
                source_id,
                task_type,
                max_retry_count,
                request_config_json,
            },
            callback,
        )
    }
}
