// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct BootstrapAdminArgs {
    pub username: String,
    pub real_name: String,
}

impl From<BootstrapAdminArgs> for super::Reducer {
    fn from(args: BootstrapAdminArgs) -> Self {
        Self::BootstrapAdmin {
            username: args.username,
            real_name: args.real_name,
        }
    }
}

impl __sdk::InModule for BootstrapAdminArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `bootstrap_admin`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait bootstrap_admin {
    /// Request that the remote module invoke the reducer `bootstrap_admin` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`bootstrap_admin:bootstrap_admin_then`] to run a callback after the reducer completes.
    fn bootstrap_admin(&self, username: String, real_name: String) -> __sdk::Result<()> {
        self.bootstrap_admin_then(username, real_name, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `bootstrap_admin` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn bootstrap_admin_then(
        &self,
        username: String,
        real_name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl bootstrap_admin for super::RemoteReducers {
    fn bootstrap_admin_then(
        &self,
        username: String,
        real_name: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            BootstrapAdminArgs {
                username,
                real_name,
            },
            callback,
        )
    }
}
