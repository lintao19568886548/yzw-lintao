// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

use super::device_asset_input_type::DeviceAssetInput;
use super::uploaded_device_image_input_type::UploadedDeviceImageInput;

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct UpdateDeviceAssetArgs {
    pub id: u64,
    pub input: DeviceAssetInput,
    pub existing_image_ids: Vec<u64>,
    pub uploads: Vec<UploadedDeviceImageInput>,
}

impl From<UpdateDeviceAssetArgs> for super::Reducer {
    fn from(args: UpdateDeviceAssetArgs) -> Self {
        Self::UpdateDeviceAsset {
            id: args.id,
            input: args.input,
            existing_image_ids: args.existing_image_ids,
            uploads: args.uploads,
        }
    }
}

impl __sdk::InModule for UpdateDeviceAssetArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `update_device_asset`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait update_device_asset {
    /// Request that the remote module invoke the reducer `update_device_asset` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`update_device_asset:update_device_asset_then`] to run a callback after the reducer completes.
    fn update_device_asset(
        &self,
        id: u64,
        input: DeviceAssetInput,
        existing_image_ids: Vec<u64>,
        uploads: Vec<UploadedDeviceImageInput>,
    ) -> __sdk::Result<()> {
        self.update_device_asset_then(id, input, existing_image_ids, uploads, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `update_device_asset` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn update_device_asset_then(
        &self,
        id: u64,
        input: DeviceAssetInput,
        existing_image_ids: Vec<u64>,
        uploads: Vec<UploadedDeviceImageInput>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl update_device_asset for super::RemoteReducers {
    fn update_device_asset_then(
        &self,
        id: u64,
        input: DeviceAssetInput,
        existing_image_ids: Vec<u64>,
        uploads: Vec<UploadedDeviceImageInput>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            UpdateDeviceAssetArgs {
                id,
                input,
                existing_image_ids,
                uploads,
            },
            callback,
        )
    }
}
