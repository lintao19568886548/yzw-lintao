// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct TransitionRepairOrderArgs {
    pub id: u64,
    pub action: String,
    pub remark: Option<String>,
}

impl From<TransitionRepairOrderArgs> for super::Reducer {
    fn from(args: TransitionRepairOrderArgs) -> Self {
        Self::TransitionRepairOrder {
            id: args.id,
            action: args.action,
            remark: args.remark,
        }
    }
}

impl __sdk::InModule for TransitionRepairOrderArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `transition_repair_order`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait transition_repair_order {
    /// Request that the remote module invoke the reducer `transition_repair_order` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`transition_repair_order:transition_repair_order_then`] to run a callback after the reducer completes.
    fn transition_repair_order(
        &self,
        id: u64,
        action: String,
        remark: Option<String>,
    ) -> __sdk::Result<()> {
        self.transition_repair_order_then(id, action, remark, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `transition_repair_order` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn transition_repair_order_then(
        &self,
        id: u64,
        action: String,
        remark: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl transition_repair_order for super::RemoteReducers {
    fn transition_repair_order_then(
        &self,
        id: u64,
        action: String,
        remark: Option<String>,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            TransitionRepairOrderArgs { id, action, remark },
            callback,
        )
    }
}
