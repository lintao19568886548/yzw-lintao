// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

#![allow(unused, clippy::all)]
use spacetimedb_sdk::__codegen::{self as __sdk, __lib, __sats, __ws};

#[derive(__lib::ser::Serialize, __lib::de::Deserialize, Clone, PartialEq, Debug)]
#[sats(crate = __lib)]
pub(super) struct OpenCarryoverBatchArgs {
    pub park_id: u64,
    pub period_label: String,
}

impl From<OpenCarryoverBatchArgs> for super::Reducer {
    fn from(args: OpenCarryoverBatchArgs) -> Self {
        Self::OpenCarryoverBatch {
            park_id: args.park_id,
            period_label: args.period_label,
        }
    }
}

impl __sdk::InModule for OpenCarryoverBatchArgs {
    type Module = super::RemoteModule;
}

#[allow(non_camel_case_types)]
/// Extension trait for access to the reducer `open_carryover_batch`.
///
/// Implemented for [`super::RemoteReducers`].
pub trait open_carryover_batch {
    /// Request that the remote module invoke the reducer `open_carryover_batch` to run as soon as possible.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and this method provides no way to listen for its completion status.
    /// /// Use [`open_carryover_batch:open_carryover_batch_then`] to run a callback after the reducer completes.
    fn open_carryover_batch(&self, park_id: u64, period_label: String) -> __sdk::Result<()> {
        self.open_carryover_batch_then(park_id, period_label, |_, _| {})
    }

    /// Request that the remote module invoke the reducer `open_carryover_batch` to run as soon as possible,
    /// registering `callback` to run when we are notified that the reducer completed.
    ///
    /// This method returns immediately, and errors only if we are unable to send the request.
    /// The reducer will run asynchronously in the future,
    ///  and its status can be observed with the `callback`.
    fn open_carryover_batch_then(
        &self,
        park_id: u64,
        period_label: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()>;
}

impl open_carryover_batch for super::RemoteReducers {
    fn open_carryover_batch_then(
        &self,
        park_id: u64,
        period_label: String,

        callback: impl FnOnce(&super::ReducerEventContext, Result<Result<(), String>, __sdk::InternalError>)
            + Send
            + 'static,
    ) -> __sdk::Result<()> {
        self.imp.invoke_reducer_with_callback(
            OpenCarryoverBatchArgs {
                park_id,
                period_label,
            },
            callback,
        )
    }
}
