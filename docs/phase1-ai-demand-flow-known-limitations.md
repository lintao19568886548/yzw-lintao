# Phase 1 AI 找房 v3 已知限制

- P0-01 测试环境运行验收仍为 `BLOCKED`；P0-02～P0-16 未启动，本次没有修改、伪造或绕过这些状态。
- 本地开发登录只在 debug build 且显式启用时可用；不是微信登录、短信认证或企业实名认证。
- 房源为脱敏内嵌 fixture，线索和会话只保存在 Rust 进程内存；进程退出后丢失。
- L0/L1 且非自营房源不会进入推荐；本地 fixture 的核验状态不代表生产数据。
- 本地规则解析覆盖 Phase 1 示例，不是完整自然语言理解；百炼路径未在隔离 E2E 中初始化。
- 未实现真实顾问分配/通知、预约带看、签约、支付、生产 SpacetimeDB 持久化或对象存储。
- 全 workspace `cargo fmt --all -- --check` 仍受本次未修改的历史文件格式差异阻塞；v3 修改的 Rust 文件单独 `rustfmt --check` 通过，未批量格式化无关文件。
- 审查包只能证明所列本地命令可复现，不代表测试环境或生产环境已经部署、联通或验收。
