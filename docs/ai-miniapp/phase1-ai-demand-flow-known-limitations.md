# Phase 1 v2 已知限制

- 房源完全虚构；不读取生产或测试 SpacetimeDB。
- 会话与线索只在 Rust 进程内存中，重启后丢失；不会通知真实顾问。
- 本地解释器是确定性规则解析，不代表完整自然语言理解；真实百炼未调用。
- 未实现真实微信登录、短信、企业实名认证、顾问分配/转派、预约带看、签约、支付或部署。
- DCloud 构建工具链有 39 个非 critical 传递依赖告警；本轮未用 `--force` 做破坏性升级。
- P0-01 运行验收仍为 `BLOCKED`；P0-02～P0-16 未启动。

镇街覆盖、元/分转换、类型化硬条件、动态日期、Pinia 恢复和隔离真实 HTTP E2E 已在 v2 整改完成，不属于接受的已知限制。
