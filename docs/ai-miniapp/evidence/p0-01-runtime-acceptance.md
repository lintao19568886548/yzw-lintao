# P0-01 测试环境运行验收

## 验收元数据

- 验收对象：P0-01“禁止生产破坏性操作”
- 整改提交：`11c1299164e2ea9f4d316da9bf7d015ad15190b0`
- 原始提交：`93c0b1c31b8db348e0a91e965dcc4eb7d0f3f7fb`
- 验收分支：`phase0/p0-01-test-runtime-acceptance`
- 检查时间：`2026-08-10T14:32:15+08:00`
- 测试环境脱敏标识：`<TEST_ENVIRONMENT_UNCONFIRMED>`
- 技术验收结论：`BLOCKED`

## 阻塞结论

```text
TEST_SSH_TRUST=BLOCKED
原因：缺少经过带外验证的测试服务器SSH别名或主机指纹
需要运维提供：SSH别名、指纹确认结果、测试环境身份确认
未访问任何服务器
```

本机存在 SSH 配置文件和 `known_hosts`，但未发现可识别的测试服务器别名，也没有可用于证明运维已带外确认主机指纹的材料。DNS 测试环境身份、测试与生产目标隔离、测试维护窗口及测试数据安全状态均无法在不连接服务器的条件下确认。依据验收安全边界，检查在任何 SSH 或服务器访问之前停止。

## Git 前置检查

| 检查 | 结果 | 退出码 |
| --- | --- | ---: |
| `git status --short` | 通过；开始时工作区干净 | 0 |
| `git branch --show-current` | 通过；开始时为 P0-01 整改分支 | 0 |
| `git rev-parse HEAD` | 通过；HEAD 为指定整改提交 | 0 |
| `git log -5 --oneline` | 通过；整改提交及原始提交历史连续 | 0 |
| `git remote -v` | 通过；唯一远程为授权的新仓库 | 0 |
| `git show --stat --oneline 11c1299164e2ea9f4d316da9bf7d015ad15190b0` | 通过；目标提交存在 | 0 |
| `git merge-base --is-ancestor 11c1299164e2ea9f4d316da9bf7d015ad15190b0 HEAD` | 通过；当前代码包含整改提交 | 0 |

已从整改提交创建独立分支 `phase0/p0-01-test-runtime-acceptance`，未修改、合并或推送 `main`，未改写已有提交历史。

## SSH 可信验证与环境隔离

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 本机 SSH 配置 | `PRESENT` | 仅确认文件存在，未记录其内容 |
| 测试 SSH 别名 | `NOT_FOUND` | 未发现可识别的测试环境别名 |
| `known_hosts` | `PRESENT` | 仅确认文件存在 |
| 测试主机指纹匹配 | `UNKNOWN` | 没有可信测试别名可供匹配 |
| 运维带外确认 | `NOT_CONFIRMED` | 本地材料不足以证明 |
| DNS 指向测试环境 | `UNKNOWN` | 未进行网络解析或访问 |
| 测试与生产隔离 | `UNKNOWN` | 未取得可信环境身份材料 |
| 总体结论 | `BLOCKED` | 禁止建立 SSH 连接 |

检查过程中未输出 SSH 别名、主机、IP、用户名、私钥路径、SSH 配置正文或指纹值，也未使用 `ssh-keyscan`、未关闭或绕过 SSH 主机校验。

## 本地回归与运行验收状态

安全规则要求在缺少可信测试 SSH 别名时立即停止。因此下列检查未执行，不能被解释为通过或失败：

| 验收项 | 状态 | 原因 |
| --- | --- | --- |
| P0-01 PowerShell 扫描器自测、路由矩阵与仓库扫描 | `NOT_RUN` | SSH 信任门禁先行阻塞，按要求立即停止 |
| Shell 语法与 Git 差异检查（运行验收阶段） | `NOT_RUN` | 同上；提交前文件检查另行执行 |
| 测试服务器只读基线 | `NOT_RUN` | 未连接服务器 |
| 后端管理端口绑定与公网直连检查 | `NOT_RUN` | 未连接服务器、未发起网络探测 |
| Nginx 完整语法、reload、active 与加载检查 | `NOT_RUN` | 未连接或修改服务器 |
| 6 条危险路由运行测试 | `NOT_RUN` | 未创建 dummy upstream，未发送请求 |
| 7 条正常路由运行测试 | `NOT_RUN` | 未创建 dummy upstream，未发送请求 |
| 两个代理入口门禁运行验证 | `NOT_RUN` | 未连接服务器 |
| SpacetimeDB CLI 2.6.1 与安全参数核验 | `NOT_RUN` | 未连接服务器，未执行 publish |
| 11 个失败关闭场景 | `NOT_RUN` | 未创建隔离沙箱 |
| Nginx 配置及软链接回滚测试 | `NOT_RUN` | 未修改服务器配置 |
| 测试前后 Nginx 配置哈希比较 | `NOT_RUN` | 未读取服务器配置 |

## 数据与生产边界声明

- 未访问任何测试或生产服务器。
- 未访问生产 DNS、安全组、数据库、对象存储、容器或服务。
- 未执行部署、服务重启、SpacetimeDB publish/delete、数据库写操作或对象存储操作。
- 未创建、修改或删除任何业务数据。
- 未读取或记录 `.env`、密码、Token、密钥、私钥、连接字符串、环境变量值或生产配置值。
- 未运行危险 HTTP 路由测试，因此不存在请求误达真实 SpacetimeDB upstream 的情况。
- 未开始 P0-02～P0-16。

## 解除阻塞所需条件

运维须通过聊天外的可信渠道提供并确认以下状态；不得在聊天、文档或日志中提供任何秘密值：

1. 已配置测试服务器 SSH 别名，并给出主机指纹的带外确认结果。
2. 明确证明该别名、DNS 目标和管理端口属于独立测试环境且与生产不重合。
3. 确认测试服务器状态正常、没有未备份的重要业务数据。
4. 确认已批准的测试维护窗口。
5. 确认当前目标 Nginx 配置可做精确单文件备份及恢复。
6. 如需公网端口核验，提供不暴露敏感信息的测试安全组只读结论或授权的外部健康探测方式。

以上条件就绪后，应重新从 SSH 信任门禁开始执行完整验收，不得沿用本报告中的 `NOT_RUN` 项作为验收证据。

## 验收状态

```text
TECHNICAL_ACCEPTANCE=BLOCKED
THREE_PARTY_SIGNOFF=PENDING
P0_01_FINAL_STATUS=阻塞，待可信测试环境条件就绪
```

技术负责人、运维负责人和业务数据负责人的签字均为待办；本报告不代替任何岗位签字，也不表示 P0-01 已最终完成。
